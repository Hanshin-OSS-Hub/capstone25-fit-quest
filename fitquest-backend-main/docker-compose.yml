services:
  web:
    container_name: fitquest-backend-web
    build: .
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    networks:
      - fitquest-net
    env_file:
      - .env

networks:
  fitquest-net:
    external: true
